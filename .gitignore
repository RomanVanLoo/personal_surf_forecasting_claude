# Commit everything EXCEPT secrets and local system junk.
# NEVER commit the GitHub token — it's a live access credential.
.github_token

# macOS / editor / local tooling junk
.DS_Store
**/.DS_Store
.claude/
